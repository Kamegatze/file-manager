server:
  port: 8081

spring:
  application:
    name: eureka-server

eureka:
  dashboard:
    path: /eureka-ui
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8081/eureka
